# Action Contract Schema

Intent Fabric defines each step as an action contract with:

- `contract_id`
- `action_type`
- `target`
- `intent`
- `simulated`
- `parameters`
- `justification`

Schema provider:

`intent_fabric.contracts.schema.action_contract_schema()`

All contracts are simulation-safe in Phase 2.
